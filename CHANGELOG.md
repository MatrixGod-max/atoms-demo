# Changelog

## v15 — 登录注册完善:用户名/邮箱双通道(2026-07-19)

- 注册支持**用户名、邮箱至少填一项**(均可后补空缺):用户名 2-20 位字母/数字/中文/
  下划线/中划线,大小写不敏感唯一;昵称独立可选,默认同用户名
- 登录统一为「账号」输入框:用户名或邮箱皆可(带 @ 走邮箱、否则走用户名),
  旧客户端 {email} 字段保持兼容
- 数据层:users.username 唯一索引(lower() 表达式索引防大小写重复);email 放开非空
  (旧库一次性重建迁移,保留全部既有列与数据,外键关系不变)
- 设置页显示用户名与邮箱(未绑定显示占位);用户菜单无邮箱时显示 @用户名
- tests/auth.test.ts:迁移后 email 可空、用户名大小写唯一;dev 冒烟 9 场景
  (双通道注册/登录、重名/格式/缺省拒绝、旧字段兼容)

## v14 — 创新四件套:聚变合成 · 验收实测 · 反馈闭环 · 指哪改哪(2026-07-19)

- ⚛ **聚变合成(Fusion Merge)**:资源中心「发现」新增聚变模式——选两个已发布应用,
  聚变分析师(Fusion Analyst)拆解两者能力产出合并蓝图(卡片入档),Engineer 以双源完整
  代码为参照重新设计出统一的新应用(非拼接);制品页「用它聚变」预选入口;首次聚变生成
  +2 积分;Remix 是复刻一个,聚变是合并两个
- ✅ **验收驱动验证**:团队模式 PM 的验收标准落库为项目属性,Validator 把它们编译成
  **声明式交互测试 DSL**(click/type/assertText/assertExists/assertNotExists,LLM 产出的是
  数据不是代码)在 headless Chrome 逐条真实执行;失败项喂回 Engineer 回炉一轮并复测;
  逐条 ✓/✗/○ 结果以验收清单卡片入档,形成「需求 → 验收标准 → 真机实测」闭环
- 📣 **反馈闭环 + 应用自愈**:发布应用自动注入 💬 访客反馈组件与运行时错误上报
  (window.onerror/unhandledrejection,会话内去重限 3 条);上报走 /api/apps/[slug]/report
  (免鉴权 + CORS + 限流 + 同文错误去重 + 每应用 200 条上限);工作台「运营」面板
  汇聚反馈/报错,属主勾选后一键「吸收反馈生成新版本」/「修复报错」(走正常生成计费,
  自愈为属主一键批准而非全自动——成本与安全考虑)
- 🎯 **指哪改哪**:预览工具条「选取元素」进入检查模式(hover 高亮、点击选中),选中元素
  以 CSS 路径 + 代码片段注入本次迭代上下文,Engineer 被约束只围绕该元素做外科手术式修改;
  检查器只注入工作台预览,发布产物不含
- 工程:vitest 单 fork 串行(消除并发 collector fork 对共享测试库的启动清理竞争,该竞争
  也会让 next build worker 踩到运行中服务,已一并加 NEXT_PHASE 守卫);tests/v13.test.ts
  7 例;smoke 新增指哪改哪/反馈闭环/聚变三步

## v13 — 目标模式:设定目标后持续构建(2026-07-19)

- 🎯 目标模式:为项目设定目标后自动持续迭代——每轮生成结束由目标评估智能体(Goal Judge)
  对照目标打分,未达标则以其缺口分析作为下一轮指令自动续跑,直到评估达标 / 轮数上限
  (默认 5,可选 3/5/8)/ 积分不足 / 用户手动停止
- 入口:首页提示卡 + 菜单「目标模式」开关(可单独填写达标标准,留空即以描述为目标);
  工作台 composer 🎯 目标 popover(保存 / 保存并持续构建)与顶部目标横幅(轮次进度、
  状态徽标、⏹ 停止 / ▶ 持续构建)
- 服务端循环驱动:jobs 在轮边界评估并链式启动下一轮(关闭页面不中断),SSE 新增
  goal_eval / goal_next 事件,前端自动跟随链式任务;进程重启自动清理孤儿循环
- 计费与限额:每自动轮按现有模式价照常扣费(流水 reason=goal-round:n),失败整轮退款;
  自动轮计入每日配额,轮上限 + 配额 + 积分三重兜底
- 目标注入:项目设定目标后,所有生成阶段提示词带【目标】块持续收敛
- 评估卡:对照目标评估(达标绿/未达标琥珀,列缺口与下一轮指令)入档消息流,自动轮
  提示带 🤖 自动迭代标记
- 附件升级:文件/文件夹双入口(文件夹自动收取其中支持的文本与图片、保留相对路径、
  跳过不支持项并提示),选择后菜单自动收起;工作台附件同步支持多选与文件夹
- 同日并行改动(另一会话):预览指哪改哪(元素点选)、验收驱动验证、聚变(两个已发布
  应用合并新项目)、运营面板(访客反馈/线上报错吸收)、发现页网格

## v12 — 用户菜单 + 积分/兑换 + 连接器真实现(2026-07-19)

- 左下角用户菜单(Atoms 风格弹层):用户设置 / 套餐 / 个人主页 / 外观 / 兑换 / 退出登录
  - 用户设置页:改名与改密码(校验当前密码,沿用注册密码策略;演示账号只读保护)
  - 套餐页:Free / Pro / Max 切换,升级一次性积分奖励(每档终身一次,降级重升不重发)
  - 个人主页 /u/[id]:公开展示用户已发布应用
  - 外观:浅色/深色/系统三态,localStorage 持久化 + 首绘前注入防闪烁,全站深色变量
  - 兑换:FUSION-XXXX-XXXX 兑换码弹窗,原子核销防重放,限流;scripts/gen-codes.mjs 生成
- 积分系统落地为真实扣费:按生成模式计费(快 1 / 混合 3 / 深度 5,深度研究/团队模式各 +1),
  注册送 20,顶部横幅可领 26,credit_events 全量流水
- 连接器真实现(免密钥):天气(open-meteo)/汇率(frankfurter)/二维码(内置 qrcode),
  统一走 /api/connectors/[name] 白名单代理(参数校验 + 10 分钟缓存 + 限流 + CORS),
  启用后向工程师提示词注入用法文档,发布应用注入 window.quark.connectors 客户端助手
- 项目自定义子域:{name}.quark-apps 域经 proxy 重写到 /d/[name],保留字与格式校验

## v11 — 入口补齐 + 主题持久化 + 语音输入(2026-07-19)

- 首页提示卡新增生成模式入口(⚡/🧠/🐢),模式随首次生成传递;工作台构建目标改为显式下拉
- 主题升级为项目属性:创建/换主题落库,首次与每次迭代由服务端注入【主题规范】持续生效,可清除
- 语音输入真实现(Web Speech API,zh-CN):首页与工作台双入口,录音脉冲/权限与兼容性提示
- 工程:dev 与生产构建目录隔离(NEXT_DIST_DIR=.next-dev),根除 dev 污染生产 .next 的隐患

## v10 — 团队模式 + 构建目标切换 + 品牌 Fusion(2026-07-19)

- 👥 团队模式构建:首次生成由 PM(需求单/用户故事/验收标准)→ Architect(界面架构蓝图)替代
  单一 Planner,迭代走 Architect 变更蓝图;需求单与蓝图以卡片入档,时间线含模型徽标
- 构建目标可切换:项目内 🌐/📱 一键切换,下次生成按新目标规范,预览/PWA 形态自动跟随
- 品牌更名 Fusion 聚变(UI 可见名;内部标识与域名保持兼容不动)
- 安全:注册密码策略加强(≥10 位含字母数字);公开 README 移除在线链接与演示账号(控制 API 成本),
  演示账号口令轮换,通过笔试文档私下提供

## v9 — Atoms 风格前端改造(2026-07-19)

- 全站浅色主题(CSS 变量整体切换),品牌 UI 更名 Atoms(内部标识不变)
- 统一应用壳:首页/资源/我的项目进入左侧边栏布局(工作区、导航高亮、空态提示、
  移动端抽屉),顶部积分横幅与 Notice 通知(纯视觉占位,可关闭)
- 新提示卡:工具菜单(附件/深度研究接真实功能;团队模式/连接器/竞赛模式视觉占位)、
  主题预设(注入首次生成提示词)、构建目标选择(网页/移动)、附件先传后进 Builder
- 深度研究可从首页直达首次生成(quark_pending 升级为 JSON,兼容旧纯文本值)

## v8 — 模型选择与阶段混合构建(2026-07-19)

- 三档生成模式:快速(全 V3)/ 混合(R1 思考 + V3 编码)/ 深度(全 R1);按阶段混用模型,
  时间线标注每阶段所用模型;模型注册表预留扩展
- 构建后预览强化:完成自动切预览 + 代码页「查看预览」提示 + 移动端完成绿点

## v7 — 资源中心 + 部署体系(2026-07-19)

- 资源中心 `/resources`:发现(社区作品)+ 模板(6 个网站/应用模板一键快速开始);模板资源
  上传 S3 专属 bucket/key;`/explore` 迁移重定向
- 统一部署:默认本机(Quark 托管)+ AWS S3 静态托管(专属 bucket,CORS 回源云存储,一键下线)
  + Netlify(实验性,自带 token)+ Vercel/CF Pages 接口预留
- Quark 服务自部署:Dockerfile + docker-compose,一条命令自托管
- 修复 CI 真根因:构建多 worker 并发初始化 SQLite 报 "database is locked"(busy_timeout 前置)

## v6 — 附件 · 深度研究 · 主题变换(2026-07-19)

- 附件上传:文本/数据文件进入智能体上下文,图片以 asset:// 引用并在落库前内联为 data URI
- 深度研究:Researcher 阶段(LLM 领域分析 + 需求内公开链接抓取,SSRF 防护),研究简报卡片注入 Planner
- 主题变换:7 个预设一键换肤,只改视觉不改功能,走版本/制品体系可回滚

## v5 — 移动端工作流 + PWA 安装制品(2026-07-19)

- 新项目类型「📱 移动应用」:Planner/Engineer 移动专属提示词(安全区/触控目标/底部拇指区/离线),
  Validator 以 390×844 触屏视口实测,工作台手机框预览;Remix 继承平台类型
- 移动制品自动 PWA 化:发布侧注入 manifest + Service Worker(离线回退,缓存随制品序号失效),
  iOS/Android 可添加到主屏幕;快照不注册 SW
- 制品页(移动):二维码扫码真机安装 + 双端安装指引;展厅/工作台平台徽标

## v4 — 制品 Artifact(2026-07-19)

- 发布升级为 registry 语义:每次发布产出不可变制品(#1、#2…),`/{slug}` 恒指 latest,
  `/{slug}/v/{n}` 为永久快照;同版本重复发布 no-op
- 公开制品页 `/artifact/{slug}`:发布历史时间线、任意快照打开/Remix、属主一键回滚 latest 指针
- 取消发布同时下线 latest 与快照(隐私优先),重新发布恢复且序号续增
- 存量已发布应用自动回填制品 #1(幂等迁移)

## v3 — 体验与产品闭环(2026-07-19)

- 移动端可用:Builder 双栏在小屏切换为「对话 / 应用」双 tab
- SSE 断流自动重连(最多 5 次,基于 job 活跃性判断),生成失败一键重试
- 展厅 `/explore`:已发布应用公开陈列(可关闭);landing 精选位
- **Remix**:任何已发布应用可一键 fork 到自己的工作台继续创造;发布应用注入可关闭的
  「⚛ 用 Quark 构建 · Remix」徽章,形成 生成→发布→被发现→被再创造 的闭环
- 演示账号 `demo@quark.dev / quark123`(只读),评审免注册可看带历史的工作台
- 取消发布、项目重命名、版本评审摘要提示、401 统一跳转、品牌 favicon、表单 autocomplete

## v2 — 生产化(2026-07-19)

- 生成作业化:任务落库 + 进程内队列(并发 2),SSE 缓冲重放断线续传,重启中断落明确失败态
- Validator 阶段:headless Chrome 真实运行产物,捕获运行时错误,自动回炉一轮修复
- 安全:发布应用独立源 `quark-apps.lexarcai.com`;预览沙箱收紧;登录/生成限流 + 24h 配额;Origin 校验
- 应用云存储:`window.quark.storage` 服务端 KV(8KB/值、64 键/应用),访客共享
- 可运维:`/api/health`、每日备份、vitest 11 例、mock 流水线、e2e 冒烟、GitHub Actions CI

## v1 — 笔试 MVP(2026-07-18)

- 注册/登录、项目 Dashboard、Planner→Engineer→Reviewer 流水线(SSE 流式)
- 沙箱实时预览、对话式迭代、版本回滚、一键发布 `/p/{slug}`
- 生产部署(systemd + Caddy 自动 HTTPS)
